"""Standalone storybook layer (v2).

A storybook is a general keepsake compiled from a person's qualifying moments,
minted ON DEMAND (Node/user-triggered) -- a legacy can hold many. Each book can
be scoped (by theme or life period) and carries 1-3 emotional tags from a fixed
registry that tone the prose and drive Node's template choice. It shares the
Sonnet assembler and the PDF renderer with the tribute, but carries no
contributor message and no cover page (the closing line is the final card).
Supports regenerate (re-render with a new preset/tags) and edit (reshape text +
scenes per cumulative instructions), mirroring the moment artifact flow.
"""
