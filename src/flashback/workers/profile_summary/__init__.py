"""Profile Summary Generator worker (step 14).

Per-person background job that produces the ``persons.profile_summary``
prose text shown at the top of a legacy page. See ARCHITECTURE.md §3.15
for the canonical spec.
"""
